interface:
  display_name: "PDF Parse Skill"
  short_description: "PDF parsing, OCR routing, extraction, and RAG packs"
  default_prompt: "Use $pdf-parse-skill to compare this PDF's extraction quality, choose the best strategy, and produce traceable outputs."
